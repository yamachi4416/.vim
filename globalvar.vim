let g:mapleader = "\<Space>"

let g:syntax_cmd = ''

" $VIMRUNTIME/plugin/matchparen.vim {{{2
let g:matchparen_timeout = 200
let g:matchparen_insert_timeout = 200

" $VIMRUNTIME/syntax/xml.vim {{{2
let g:xml_syntax_folding = 1

" $VIMRUNTIME/indent/html.vim {{{2
let g:html_indent_inctags = 'html,body,head,tbody'
let g:html_indent_autotags = 'th,td,tr,tfoot,thead'

" $VIMRUNTIME/indent/vim.vim {{{2
let g:vim_indent_cont = 0

" $VIMRUNTIME/syntax/vim.vim {{{2
let g:vimsyn_embed = ''
let g:vimsyn_folding = 'af'
let g:vimsyn_noerror = 1

" $VIMRUNTIME/autoload/htmlcomplete.vim {{{2
let g:uppercase_tag = 0

" $VIMRUNTIME/autoload/rubycomplete.vim {{{2
let g:rubycomplete_rails = 1
let g:rubycomplete_classes_in_global = 1
let g:rubycomplete_buffer_loading = 1
let g:rubycomplete_include_object = 1
let g:rubycomplete_include_objectspace = 1

let g:ruby_space_errors = 1
let g:ruby_fold = 1
